# Hometime Stage 4: board search

1. Add a deterministic, browser-safe fuzzy ranking helper that preserves manual order for ties.
2. Add an accessible client search and results view with local query/highlight state, keyboard shortcuts, wrapping navigation, new-tab activation, and empty states.
3. Integrate it into the existing public board page so default-board redirects use the same behavior.
4. Add focused ranking and interaction regression coverage, then run typecheck and tests, inspect the diff, and commit the Stage 4 change.
